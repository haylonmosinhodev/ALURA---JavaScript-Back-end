const numeros = [100, 200, 300, 400, 500, 600];

//Primeira expressao executada somente uma vez. let indice = 0
// Segunda expressa é uma condicao de expressa
// terceira expressa é executada sempre ao final do bloco.


for (let indice = 0; indice < numeros.length; indice++){
    
console.log (numeros[(indice)]);

}
